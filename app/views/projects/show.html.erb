<div class="container">
app\views\projects\show.html.erb
  <%= render @project %>

  <div>
    <%= link_to "Edit this project", edit_project_path(@project), class: "btn btn-primary" if current_user&.admin? %> |
    <%= link_to "Back to projects", projects_path, class: "btn btn-primary" %>
    <%= button_to "Destroy this project", @project, method: :delete, class: "btn btn-primary" if current_user&.admin?   %>
  </div>
</div>